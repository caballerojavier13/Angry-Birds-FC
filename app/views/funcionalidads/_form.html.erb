<%= form_for(@funcionalidad) do |f| %>
  <% if @funcionalidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funcionalidad.errors.count, "error") %> prohibited this funcionalidad from being saved:</h2>

      <ul>
      <% @funcionalidad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
